---
import { Image } from 'astro:assets';
import type { ImageMetadata } from 'astro';

interface Props {
  title: string;
  description?: string | undefined;
  cta: { label: string; href: string };
  image: ImageMetadata;
  imageAlt: string;
}

const { title, description, cta, image, imageAlt } = Astro.props;
---

<article class="card-cta">
  <Image
    src={image}
    alt={imageAlt}
    class="card-cta__bg"
    loading="lazy"
    widths={[480, 768]}
    sizes="(max-width: 600px) 100vw, 33vw"
  />
  <div class="card-cta__overlay" aria-hidden="true"></div>
  <div class="card-cta__content">
    <h3 class="card-cta__title">{title}</h3>
    {description && <p class="card-cta__desc">{description}</p>}
    <a href={cta.href} class="card-cta__btn">
      {cta.label}
    </a>
  </div>
</article>

<style>
  .card-cta {
    position: relative;
    overflow: hidden;
    border-radius: var(--vrm-radius-lg);
    min-height: 380px;
    display: flex;
    align-items: center;
    justify-content: center;
    text-align: center;
    box-shadow: var(--vrm-shadow-md);
  }

  .card-cta__bg {
    position: absolute;
    inset: 0;
    width: 100%;
    height: 100%;
    object-fit: cover;
    transition: transform var(--vrm-transition-slow);
  }

  .card-cta:hover .card-cta__bg {
    transform: scale(1.04);
  }

  .card-cta__overlay {
    position: absolute;
    inset: 0;
    background: linear-gradient(
      to bottom,
      rgb(0 0 0 / 0) 0%,
      rgb(0 0 0 / 0.18) 50%,
      rgb(0 0 0 / 0.4) 100%
    );
  }

  .card-cta__content {
    position: relative;
    z-index: 1;
    width: 100%;
    padding: var(--vrm-space-lg);
    display: flex;
    flex-direction: column;
    gap: var(--vrm-space-md);
    align-items: center;
    text-align: center;
  }

  .card-cta__title {
    font-size: var(--vrm-font-size-xl);
    color: var(--vrm-color-text-inverted);
    margin-bottom: 0;
    text-shadow: 0 1px 4px rgb(0 0 0 / 0.7), 0 0 16px rgb(0 0 0 / 0.4);
  }

  .card-cta__desc {
    color: rgb(255 255 255 / 0.96);
    font-size: var(--vrm-font-size-sm);
    line-height: var(--vrm-line-height-relaxed);
    margin: 0;
    max-width: 32ch;
    text-shadow: 0 1px 4px rgb(0 0 0 / 0.7), 0 0 12px rgb(0 0 0 / 0.4);
  }

  .card-cta__btn {
    display: inline-flex;
    align-items: center;
    padding: var(--vrm-space-xs) var(--vrm-space-md);
    border: 2px solid var(--vrm-color-text-inverted);
    color: var(--vrm-color-text-inverted);
    font-size: var(--vrm-font-size-sm);
    font-weight: var(--vrm-font-weight-semibold);
    border-radius: var(--vrm-radius-full);
    text-decoration: none;
    backdrop-filter: blur(4px);
    background-color: rgb(255 255 255 / 0.12);
    transition:
      background-color var(--vrm-transition-fast),
      color var(--vrm-transition-fast);

    &:hover,
    &:focus-visible {
      background-color: var(--vrm-color-text-inverted);
      color: var(--vrm-color-primary);
      text-decoration: none;
    }
  }

  @media (prefers-reduced-motion: reduce) {
    .card-cta__bg {
      transition: none;
    }
  }
</style>
